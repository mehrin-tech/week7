🎯 Task

Write an aggregation pipeline to:

1️⃣ Calculate total amount for each order
  (amount = price × qty)

2️⃣ Group by category

3️⃣ Find total sales amount per category

4️⃣ Sort by total sales (highest first)

5️⃣ Show only top 2 categories


sample> db.orders.aggregate([
...   {
...     $project: {
...       customer: 1,
...       category: 1,
...       price: 1,
...       qty: 1,
...       totalAmount: { $multiply: ["$price", "$qty"] }
...     }
...   },
...   {
...     $group: {
...       _id: "$category",
...       totalSales: { $sum: "$totalAmount" }
...     }
...   },
...   { $sort: { totalSales: -1 } },
...   { $limit: 2 }
... ])
[
  { _id: 'Electronics', totalSales: 50000 },
  { _id: 'Clothing', totalSales: 8500 }
]
sample>

2)🎯 Task

Write an aggregation pipeline to:

1️⃣ Show only delivered orders
2️⃣ Calculate order amount (price × qty)
3️⃣ Group by customer
4️⃣ Find total amount spent by each customer
5️⃣ Sort by total spent (highest first)


sample> db.orders.aggregate([{$match:{status:"delivered"}},{$project:{custom
sample> db.orders.aggregate([{$match:{status:"delivered"}},{$project:{customer:1,category:1,price:1,qty:1,status:1,orderAmount:{$multiply:["$price","$qty"]}}},{$group:{_id:"$customer",totalAmount:{$sum:"$orderAmount"}}},{$sort:{totalAmount:-1}}])
[
  { _id: 'Asha', totalAmount: 12000 },
  { _id: 'Nimal', totalAmount: 8000 },
  { _id: 'Rahul', totalAmount: 4500 }
]
sample>


🎯 Task

Write an aggregation pipeline to:

1️⃣ Calculate orderAmount = price × qty
2️⃣ Show only orders where orderAmount > 10,000
3️⃣ Group by category
4️⃣ Count how many high-value orders in each category
5️⃣ Sort by count (highest first)


sample> db.orders.aggregate([{$project:{customer:1,category:1,price:1,qty:1,orderAmount:{$multiply:["$price","$qty"]}}},{$match:{$expr:{$gt:["$orderAmount",5000]}}},{$group:{_id:"$category",highValueOrders:{$sum:1}}},{$sort:{hig
highValueOrders:-1}}])
[ { _id: 'Electronics', highValueOrders: 6 } ]


Q)🎯 Task

Write an aggregation pipeline on students to:

1️⃣ Join class details using $lookup
2️⃣ Show only classes handled by teacher "Suma"
3️⃣ Group by class name
4️⃣ Count how many students in each class
5️⃣ Output format:

db.students.aggregate([{$lookup:{
    from:"classes",
    localField:"classId",
    foreignField:"_id",
    as:"class"
}},{$unwind:"$class"},{$match:{"class.teacher":"Suma"}},{$group:{_id:"$class.className",totalStd:{$sum:1}}}
])

[ { _id: 'Plus One', totalStd: 2 } ]